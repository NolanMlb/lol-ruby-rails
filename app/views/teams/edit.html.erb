<h1 class="text-2xl font-bold mb-4">Modifier l’équipe : <%= @team.name %></h1>

<%= render 'form', team: @team %>

<%= link_to '⬅️ Retour', team_path(@team), class: "text-blue-600 hover:underline mt-4 inline-block" %>